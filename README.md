# Inverted-Search
A C-based Inverted Search application that uses hash tables and linked lists to index words across multiple text files, supporting database creation, search, display, save, and update operations.
